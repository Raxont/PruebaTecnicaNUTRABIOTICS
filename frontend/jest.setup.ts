/**
 * jest.setup.ts
 * Se ejecuta antes de cada archivo de test.
 * Importa los matchers de @testing-library/jest-dom
 * (toBeInTheDocument, toHaveValue, etc.)
 */
import '@testing-library/jest-dom';